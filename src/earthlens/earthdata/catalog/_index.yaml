# Auto-generated index of CMR collections per provider (regenerated by
# tools/earthdata/refresh_earthdata_catalog.py — C7). Informational only;
# runtime code resolves against the curated `datasets:` blocks in the
# per-DAAC files. Seeded here with the curated keys; C7 grows the long tail.
available_datasets:
  - GPM_3IMERGHHL_07
  - GPM_3IMERGM_07
  - MUR-JPL-L4-GLOB-v4.1
  - GEDI04_A_002
